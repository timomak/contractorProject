<h1>New Posts</h1>
<!-- <div class="container">
  {{{body}}}
</div> -->
<div class="row">
  <div class="col-sm-8 col-sm-offset-2">
    <ul>
      {{#each posts}}
      <li class="list-group-item">
        <div class="lead">
          <a href="/posts/{{this._id}}" class="lead">{{this.title}}</a>
        </div>
        <div class="text-right">
          <a href="/users/{{this.authorUsername}}">{{this.authorUsername}}</a>
        </div>
        <a href="https://{{this.url}}" target="_blank">{{this.url}}<a>
          <div class="text-right">
            <a href="/n/{{this.subreddit}}">{{this.subreddit}}</a>
          </div>
      </li>
      {{/each}}
    </ul>
  </div>
</div>
